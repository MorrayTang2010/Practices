# 在 R 的软件包 MASS 中有数据 survey, 是对237名统计专业的学生的调查问卷数据。
# 对其中的变量 Pulse 和 Height，完成下列任务：
# a)	画出两个变量的散点图；
# b)	建议一个包含若干参数的非线性函数来拟合两个变量的关系； 
# c)	由 nls 函数计算出参数的估计值；
# d)	计算此次拟合的残差平方和；
# e)	将此非参数曲线加到上述的散点图上。
